<p class="karaokebar">KaraokeBar</p>
<style>
  .karaokebar {
    display: flex;
    font-size: 2rem;
    align-self: center;
    justify-self: center;
    color: var(--txt-primary);
    transition: all 0.2s ease-in-out;
  }
  .karaokebar:hover {
    animation: grow 1.5s ease-in-out infinite;
  }

  @media (max-width: 768px) {
    .karaokebar {
      animation: grow 1.5s ease-in-out infinite;
    }
  }

  @keyframes grow {
    0% {
      transform: scale(1);
      color: var(--txt-primary);
    }
    33% {
      color: #0ea13f;
    }
    50% {
      transform: scale(1.2);
      color: #1929b3;
    }
    66% {
      color: #910803;
    }
    100% {
      transform: scale(1);
      color: var(--txt-primary);
    }
  }
</style>
